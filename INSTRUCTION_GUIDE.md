# 📘 Руководство по настройке бота

## Структура файла

```
data.json
└── cards (массив карточек)
    └── card (объект карточки)
        ├── id, title, description
        ├── buttonText, buttonAction
        ├── websiteUrl, websiteText
        └── instruction
            ├── title, steps, footer
            └── steps[] (массив шагов)
                ├── type: "links", "list" или "text"
                ├── title, text
                ├── links[] (для type: "links")
                ├── items[] (для type: "list")
                └── text (для type: "text")
```

## Поля карточки

| Поле | Тип | Описание | Пример |
|------|-----|----------|--------|
| `id` | string | Уникальный ID (латиница) | `"amnezia_wg"` |
| `title` | string | Заголовок. `NEW!` → красный бейдж | `"NEW! AmneziaWG"` |
| `description` | string | Описание сервиса | `"VPN для всех устройств"` |
| `buttonText` | string | Текст кнопки | `"📖 Инструкция"` |
| `buttonAction` | string | Действие кнопки | `"openInstruction"` |
| `websiteUrl` | string | Ссылка на сайт | `"https://..."` |
| `websiteText` | string | Текст кнопки сайта | `"Сайт"` |
| `instruction` | object | Инструкция по настройке | см. ниже |

## Поля инструкции

| Поле | Тип | Описание |
|------|-----|----------|
| `title` | string | Заголовок инструкции |
| `steps` | array | Массив шагов (объекты) |
| `footer` | string | Финальный текст (опционально) |

## Типы шагов

### 1. `links` — кнопки-ссылки

Используется для скачивания, файлов, документов.

```json
{
  "title": "Шаг 1: Скачивание",
  "type": "links",
  "text": "Выберите платформу:",
  "links": [
    {"name": "🪟 Windows", "url": "https://..."},
    {"name": "📄 Конфиг", "url": "https://..."},
    {"name": "📖 Документация", "url": "https://..."}
  ]
}
```

**Поля:**
- `name` — текст кнопки (можно с эмодзи)
- `url` — ссылка

### 2. `list` — нумерованный список

Используется для текстовых инструкций.

```json
{
  "title": "Шаг 2: Подключение",
  "type": "list",
  "items": [
    "Откройте приложение",
    "Нажмите «Добавить»",
    "Вставьте ключ"
  ]
}
```

**Поля:**
- `items` — массив строк

### 3. `text` — простой текст

Используется для информационных сообщений без ссылок и списков.

```json
{
  "title": "Шаг 3: Информация",
  "type": "text",
  "text": "Это пример шага с простым текстом. Здесь нет ссылок или нумерованного списка — только обычный текст инструкции."
}
```

**Поля:**
- `text` — строка с текстом инструкции

## Форматирование текста

Во всех типах шагов поддерживается **Markdown-синтаксис**:

### Выделение текста

| Синтаксис | Результат | Пример ввода | Как выглядит |
|-----------|-----------|--------------|--------------|
| `**текст**` | **Жирный** | `**важно**` | **важно** |
| `__текст__` | **Жирный** | `__внимание__` | **внимание** |
| `*текст*` | *Курсив* | `*примечание*` | *примечание* |
| `_текст_` | *Курсив* | `_акцент_` | *акцент* |
| `==текст==` | ==Выделение маркером== | `==нажмите сюда==` | ==нажмите сюда== |
| `` `текст` `` | `Код` | `` `config.json` `` | `config.json` |

### Перенос строк

| Способ | Пример | Результат |
|--------|--------|-----------|
| `\n` | `"строка 1\nстрока 2"` | Две строки |
| `<br>` | `"строка 1<br>строка 2"` | Две строки |
| `\n\n` | `"абзац 1\n\nабзац 2"` | Два абзаца с пустой строкой |

**Примеры:**

```json
{
  "title": "Шаг с форматированием",
  "type": "text",
  "text": "Нажмите ==кнопку Подключить__\nЗатем введите `пароль` из письма.\n\n**Важно:** не сообщайте пароль другим!"
}
```

```json
{
  "title": "Шаг 2: Список",
  "type": "list",
  "items": [
    "Откройте файл `config.json`",
    "Скопируйте ==ключ доступа==",
    "Вставьте в приложение"
  ]
}
```

## Примеры

### Добавить новую карточку

```json
{
  "id": "outline_vpn",
  "title": "Outline VPN",
  "description": "Простой VPN от Jigsaw",
  "buttonText": "📖 Инструкция",
  "buttonAction": "openInstruction",
  "websiteUrl": "https://getoutline.org",
  "websiteText": "Сайт",
  "instruction": {
    "title": "📖 Настройка Outline",
    "steps": [
      {
        "title": "Шаг 1: Скачивание",
        "type": "links",
        "text": "Выберите платформу:",
        "links": [
          {"name": "🪟 Windows", "url": "https://..."},
          {"name": "🤖 Android", "url": "https://..."}
        ]
      },
      {
        "title": "Шаг 2: Ключ доступа",
        "type": "list",
        "items": [
          "Скопируйте ключ из канала",
          "Вставьте в приложение"
        ]
      }
    ],
    "footer": "✅ Готово!"
  }
}
```

### Добавить ссылку на конфиг

В секцию `links` добавьте:

```json
{"name": "📄 Файл конфигурации", "url": "https://your-site.com/config.json"}
```

### Удалить карточку

Просто удалите соответствующий объект из массива `cards`.

## Советы

1. **Порядок карточек** — как в массиве, сверху вниз
2. **Порядок шагов** — отображаются последовательно
3. **Пустые поля** — `websiteUrl` и `websiteText` можно оставить `""`
4. **Эмодзи** — поддерживаются везде
5. **Бейдж NEW!** — автоматически, если `title` начинается с `NEW!`
6. **Форматирование** — используйте Markdown для выделения важного
